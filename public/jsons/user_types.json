{
    "fields": [
        {
            "name": "user_type",
            "type": "string"
        },
        {
            "name": "status",
            "type": "select",
            "options": {
                "1": "Active",
                "2": "Inactive",
                "0": "Deleted"
            }
     
      }
    ],
    "validations": [
        {
            "field": "user_type",
            "rules": "required|max:50"
        }
    ]
}